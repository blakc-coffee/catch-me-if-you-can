import { ArrowTopRightIcon } from "@radix-ui/react-icons";
import { MobileScroll, type FlowControls } from "../mobile";
import { NavBar } from "./shared";
export function MissionScreen({ flow, onScanner, onIntel }: { flow: FlowControls; onScanner: () => void; onIntel: () => void }) {
  return <div className="screen-shell mission-shell"><div className="mission-header"><span>OPENVERSE</span><i>/</i><small>SEEKER</small></div><MobileScroll className="app-screen"><main className="screen-content mission-content"><section className="mission-hero"><h1>Good evening, Seeker.</h1><p>Explore the campus. Find the artifacts.</p></section><section className="mission-status-card" aria-label="Live mission status"><div className="mission-live"><i /> LIVE MISSION</div><strong>10 / 15</strong><span>artifacts claimed</span><div className="mission-divider" /><div className="mission-meta"><small>FIELD STATUS</small><small>ACTIVE</small></div></section><div className="mission-section-label">NEXT OBJECTIVE</div><section className="mission-objective-card"><div className="objective-kicker">QR-11 <i>/</i> CAMPUS INTEL</div><h2>Find the next marker</h2><p>Scan its QR code to reveal the challenge.</p></section><button className="mission-open-button" type="button" onClick={onScanner}>Open scanner <ArrowTopRightIcon /></button><section className="position-sharing"><div>POSITION SHARING</div><p>Your live location is visible to command.</p></section></main></MobileScroll><NavBar active="mission" onIntel={onIntel} /></div>;
}
