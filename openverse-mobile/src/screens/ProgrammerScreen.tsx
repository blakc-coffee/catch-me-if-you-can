import { useState } from "react";
import { ArrowRightIcon, CheckCircledIcon, LockClosedIcon } from "@radix-ui/react-icons";
import { KeyboardInput, MobileScroll, type FlowControls } from "../mobile";
import { BrandHeader, NavBar } from "./shared";
export function ProgrammerScreen({ flow, onMission }: { flow: FlowControls; onMission: () => void }) {
  const [answer, setAnswer] = useState(""); const [status, setStatus] = useState<"idle" | "wrong" | "correct">("idle");
  return <div className="screen-shell"><BrandHeader flow={flow} caseLabel="SEEKER / CASE 01" /><MobileScroll className="app-screen"><main className="screen-content case-content"><div className="case-index">CASE 01 / 03</div><h1>The Programmer</h1><section className="glass-card case-file"><span className="card-kicker">CASE FILE 01 / IDENTITY</span><p>Sherlock has found a programmer, but his name has been removed. Find him using these clues:</p><ol type="I"><li>He created a web framework whose philosophy includes optimizing for programmer happiness.</li><li>His framework became known for Convention over Configuration.</li><li>He later created an opinionated Linux distribution.</li><li>He is commonly referred to by three initials.</li></ol><strong>Who is he? Enter three initials, not the full name.</strong></section>{status === "correct" ? <section className="success-panel"><CheckCircledIcon /><span>CASE VERIFIED</span><h2>DHH accepted.</h2><p>Artifact 05 has been added to your recovered set.</p><button className="primary-action" type="button" onClick={onMission}>Return to mission <ArrowRightIcon /></button></section> : <section className="answer-panel"><label htmlFor="case-answer">YOUR ANSWER <span>/ THREE INITIALS</span></label><KeyboardInput id="case-answer" value={answer} maxLength={3} autoCapitalize="characters" placeholder="Enter answer" onChange={(event) => { setAnswer(event.target.value); setStatus("idle"); }} />{status === "wrong" ? <p className="error-message"><LockClosedIcon /> Answer rejected. Recheck the clues.</p> : null}<button className="primary-action" type="button" onClick={() => setStatus(answer.trim().toUpperCase() === "DHH" ? "correct" : "wrong")} disabled={answer.trim().length !== 3}>Submit answer <ArrowRightIcon /></button></section>}</main></MobileScroll><NavBar active="cases" /></div>;
}
